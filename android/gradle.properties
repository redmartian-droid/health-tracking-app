# This file is used to configure Gradle properties for your Android project.

# Enables the use of custom BuildConfig fields, which is required by some
# older React Native libraries like react-native-vector-icons.
android.defaults.buildfeatures.buildconfig=true

# Other common properties you might need:
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.enableJetifier=true
android.suppressUnsupportedCompileSdk=34
